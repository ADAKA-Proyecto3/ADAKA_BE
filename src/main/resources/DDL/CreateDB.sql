CREATE DATABASE  ADAKA_DB;

CREATE TABLE USER  (
    id INTEGER GENERATED BY DEFAULT AS IDENTITY,
    email VARCHAR(255),
    name VARCHAR(255),
    password VARCHAR(255),
    phone VARCHAR(255),
    role VARCHAR(255),
    status VARCHAR(255),
    PRIMARY KEY (id)
    );



